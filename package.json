{
  "name": "fractional-indexing",
  "version": "2.0.0",
  "description": "Provides functions for generating ordering strings",
  "main": "out/index.js",
  "engines": {
    "node": ">=13"
  },
  "scripts": {
    "build": "rm -rf out && tsc && prettier out/* --write",
    "format": "prettier *.json src/ --write",
    "prepare": "npm run build && npm run format",
    "test": "node src/test.js"
  },
  "keywords": [
    "fractional",
    "indexing",
    "ordering",
    "order",
    "realtime"
  ],
  "author": "arv@roci.dev",
  "license": "CC0-1.0",
  "devDependencies": {
    "prettier": "^2.1.2",
    "typescript": "^4.0.5"
  }
}
